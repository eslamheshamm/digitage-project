import React from "react"

const Button = ({ children }) => (
  <button className="py-4 px-8 tracking-wider	 font-title font-bold rounded-lg bg-primary uppercase">
    {children}
  </button>
)
export default Button
